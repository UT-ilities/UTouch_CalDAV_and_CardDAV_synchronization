USERNAME=''
PASSWORD=''
DOMAIN='domain.tld'
PORT='443'

NEXTCLOUD_CAL_TEMPLATE="https://$DOMAIN/remote.php/dav"
CAL_URL="$NEXTCLOUD_CAL_TEMPLATE"

CALENDAR_SERVER_CONFIG_NAME="nextcloud-calendar" # can NOT include the '@' symbol, can NOT be greater than 31 characters!
CALENDAR_NAME="$USERNAME-$DOMAIN"                # can NOT include the '@' symbol, can NOT be greater than 31 characters!
CALENDAR_VISUAL_NAME="$USERNAME - nextcloud"     # can NOT include the '@' symbol, can NOT be greater than 31 characters!

NEXTCLOUD_CONTACTS_TEMPLATE="https://$DOMAIN/remote.php/dav/addressbooks/users/$USERNAME/contacts"
CONTACTS_URL="$NEXTCLOUD_CONTACTS_TEMPLATE"

CONTACTS_SERVER_CONFIG_NAME="nextcloud-contacts" # can NOT include the '@' symbol, can NOT be greater than 31 characters!
CONTACTS_NAME="$USERNAME-$DOMAIN"                # can NOT include the '@' symbol, can NOT be greater than 31 characters!
CONTACTS_VISUAL_NAME="$USERNAME - nextcloud"     # can NOT include the '@' symbol, can NOT be greater than 31 characters!

CRON_FREQUENCY='0,15,30,45 * * * *' # every 15 minutes
#CRON_FREQUENCY='0,30 * * * *'       # every 30 minutes
#CRON_FREQUENCY='0 * * * *'          # every hour
#CRON_FREQUENCY='@hourly'            # non standard
